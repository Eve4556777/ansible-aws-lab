---
- name: Install Apache Web Server
  hosts: localhost
  connection: local
  become: yes

  tasks:
    - name: Install httpd
      yum:
        name: httpd
        state: present

    - name: Start service
      service:
        name: httpd
        state: started
        enabled: true
